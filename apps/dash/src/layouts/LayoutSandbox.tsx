/*import { Dashboard, DashboardProps, Navbar, Sidebar, NavbarProps, SidebarProps } from "@ui8kit/core";

export function NavbarSandbox({ isDarkMode, toggleDarkMode, brand = "SandBox AI" }: NavbarProps) {
  return <Navbar isDarkMode={isDarkMode} toggleDarkMode={toggleDarkMode} brand={brand} />;
}

export function SidebarSandbox({ children, className, dataClass, title }: SidebarProps) {
  return <Sidebar className={className} dataClass={dataClass} title={title}>{children}</Sidebar>;
}

export function LayoutSandbox({ page, children, sidebar, navbarProps }: DashboardProps) {
  return (
    <Dashboard page={page} sidebar={sidebar} navbarProps={navbarProps}>
      {children}
    </Dashboard>
  );
}

*/